name: Plan Validation

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'plan/**'
      - 'scripts/ruby/plan/**'
      - '.github/workflows/plan.yml'
  push:
    branches:
      - 'release/**'
    paths:
      - 'plan/**'
      - 'scripts/ruby/plan/**'
      - '.github/workflows/plan.yml'
  workflow_dispatch:

concurrency:
  group: plan-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Plan Structure
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Validate plan structure
        run: ruby scripts/ruby/plan/index.rb plan
