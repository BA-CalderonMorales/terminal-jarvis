[advisories]
ignore = [
    "RUSTSEC-2025-0134", # rustls-pemfile via libsql - waiting for upstream fix
    "RUSTSEC-2025-0141", # bincode 1.3.3 via libsql - unmaintained, no upstream fix yet
]

# Known vulnerabilities without RUSTSEC IDs (tracked via GHSA/CVE):
# - GHSA-8m95-fffc-h4c5 / CVE-2025-47736: libsql-sqlite3-parser 0.13.0 crash on invalid UTF-8
#   Severity: Low (CVSS 2.9), DoS only, no official fix released yet
#   Fix commit exists (14f422a) but not yet published to crates.io
#   Waiting for libsql upstream to release updated version
